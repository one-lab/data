version 1.0-SNAPSHOT
功能：
1.支持基本更新
2.支持基本查询
3.<T> T insert(T entity)
4.<T> void update(T entity)
5.<T> void delete 通过ID删除
6.<T> T find(Object id) 通过ID查询
7.<T> List<T> findAll() 查询所有
8.支持默认UUID生成策略(UUID)

version 1.1-SNAPSHOT
功能：
1.支持ID生成策略
2.支持批量更新
3.支持通过ID批量查询<T> List<T> findAllByIds(Collection ids) 根据ID查询所有
